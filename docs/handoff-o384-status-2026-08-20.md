# 工作交接：carrier 流 VeryHard 攻坚（O384b 快照，2026-08-20）

> 本文件取代 `docs/handoff-o379-bisect-2026-08-20.md` 作为当前开工入口。
> 历史 bisect 判决仍有效：Zerg Rush 高方差主导，O377-O379 不回滚。
> 完整逐局证据见 `docs/battle-log.md` 文末。

## 一、当前仓库/运行状态

- 分支：`develop`
- HEAD：`76b854a o384b 区分首塔资金与落点失败并放宽长局`
- 远端：已 push，worktree clean。
- 测试：**881 tests OK，skip=1**；`bot.main` import smoke 通过。
- 当前无 bench/run.py/SC2 活跃进程；可安全启动下一轮。
- bench 默认协议：headless 双 lane、启动锁串行化“启动→首 state”，
  正式对局仍并行；pid 后首 state 窗 120s；单局 wall timeout **7200s**。

## 二、本轮有效结果

### Zerg Rush

- `o382c game_01`：**Victory 958.9s**
  - 二矿 269.7s、三矿 500.4s、四矿 654.6s；
  - 农峰70，终局64农/4基地；
  - 终局暴风20+航母4，200/200。
- `o382c game_02`：**无效样本，不计胜负**
  - 约1539s仍6基地/67农/暴风21+航母4，游戏健康推进；
  - bench 3600s wall 到期后清空 state 自动重试，故只保留已记录指标。
- 结论：O382 已把“舰队未成型即早崩”迁出；最新有效样本 1/1，
  仍需按两轮累计，不能据此宣布水位提升。

### Terran Rush

- `o382d game_01`：Defeat 963.1s。
- `o382d game_02`：Defeat 1560.7s。
- 正向迁移：
  - 二矿从277s改善到249s；
  - 首SG从约394s提前到225-293s；
  - 500s舰队从1提高到2；
  - 丢矿后SG不再清零，舰队峰14；
  - 1064.6s首次事件级触发“Terran制空后主动斩断分矿”。
- 剩余死因：舰队出击时自家矿区被10-15地面小队逐矿切断，农民68→29→0；
  预接触塔帽存在PSD目标旁路；安全重建选到离家过远矿点。

## 三、O382-O384b 已落地机制

1. **首扩/丢矿 Nexus 独占基金**
   - 首扩220s启动，截断仍消费的 BuildOrderRunner；
   - 基金统一堵非生存结构、科技 watchdog、直产漏口；
   - 丢矿恢复曾从221-265s缩短到约20s。
2. **健康矿区**
   - 采矿位口径修为“实时剩余矿点×2”（矿点 ideal_harvesters 实测为0）；
   - 45+农目标3片健康矿区；
   - 触发后 latch 到 Nexus 实体数+1，期间禁止 O307 撤派。
3. **Terran 假 rush**
   - 首接触/420s前地面保底≤2；
   - O384 起最终F2/PSD目标层：主基/每分矿各≤1塔/1电池；
   - 首接触后自动解除。
4. **Terran 安全重建/产能恢复**
   - 有可见敌地面时选“距敌远、距主基≤80且带0.5回防权重”的扩张点；
   - Nexus 恢复后120s只留新矿首塔，升级、第2+塔/电池和额外产能让位
     BY→SG→FB。
5. **Terran 制空经济打击**
   - t≥720、舰队≥8、敌可见空军和硬AA为0、已知敌基地≥2时，
     目标改最外围已知分矿；
   - O384 起经济打击召回门封顶10（普通推进14/25不动）。
6. **塔/产能投资**
   - 全局塔绝对顶18，新矿零塔首座生存塔豁免；
   - 首舰真实出场前不允许第3+SG（pending不再冒充在场）。
7. **首塔基金**
   - 90s最大窗；
   - O384b：年龄≥15s、已买得起塔且无tracker/in-flight时，
     判定为placement/no_worker/tech失败，立即释放全局基金，15s后重试。
8. **bench 稳定性**
   - 跨进程 launch lock 修“核心：访问许可错误”；
   - replay 每lane/game独立路径；
   - 首state冷启动窗120s；
   - wall timeout 7200s。

## 四、下一步（O385，按优先级）

1. 新标签双 lane：
   - lane1：VH Zerg Rush ×3（每局结束暂停尸检）；
   - lane2：VH Terran Rush ×3（每局结束暂停尸检）。
2. Terran game_03 强制验收：
   - 首接触/420s前最终每基地塔/电池≤1；
   - 二矿<300s、首SG约225-300s、500s舰队≥2；
   - 制空斩分矿后基地10地面威胁立即回防；
   - 丢矿目标距主基≤80，不再选(160,100)类远角；
   - 恢复后SG不清零。
3. Zerg game_03 强制验收：
   - 健康扩张 latch 期间零O307撤派；
   - 首塔非资金失败事件能释放基金，首舰不晚于619s；
   - 满人口长局若仍不终结，统计O302、腐化信用、敌基地数和实际攻击目标，
     将“舰队活着但结束不了”作为下一主病灶。
4. 每局流程不变：
   `autopsy_summary.py` + replay bases + state/run.log；
   写至少3项数据化改进→纯函数/单测→全量测试/import smoke→下一局。

## 五、不可回滚/仍有效纪律

- O377-O379（在场口径、虚空cap2、腐化硬闸、塔降级）保留。
- 单轮0/3或2/3不能判升降；两轮累计+滚动窗口；疑回归先bisect复跑。
- Zerg Timing、Terran Power仍封存，除非司令复议。
- 不得用全局塔数替代每基地局部防御验收。
