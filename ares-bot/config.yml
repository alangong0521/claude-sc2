# Check `ares-sc2/src/ares/config.yml` for valid config options
# Use this to override those values
# Can add own unique config options here if desired

# Custom values not used by ares.
# NOTE: the KEYS `MyBotName`/`MyBotRace` are read by run.py — keep them.
# Only the VALUES matter: MyBotName's value is the in-game bot name.
MyBotName: CodeAgentSC2
MyBotRace: Protoss
########################

UseData: False
Debug: False
GameStep: 2
DebugGameStep: 2

DebugOptions:
    # one of: Air, AirVsGround, Ground, GroundAvoidance, AirAvoidance
    ActiveGrid: Ground
    ChatDebug: True
    DebugSpawn: False
    ShowPathingCost: False
    ResourceDebug: False
